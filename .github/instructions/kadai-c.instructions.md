---
applyTo: "kadai-c/**"
---

**Problem statement:** Create a shell script `concatenation.sh` that assembles split PDF files under `splits/` into a single PDF named `1.pdf`.

**Requirements:**

- Nothing in particular.
